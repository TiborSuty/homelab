name: ARC smoke test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner:
    name: Verify homelab runner
    runs-on: homelab-runners
    timeout-minutes: 5
    steps:
      - name: Show runner environment
        run: |
          echo "ARC runner is online"
          uname -a
