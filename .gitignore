# Talos machine configurations contain cluster private keys.
/controlplane.yaml
/worker.yaml
/worker-1.yaml
/.local/
/talos/base/
/talos/rendered/
/talos/legacy/

# OpenTofu working data, plans, local state, and uncommitted variable files.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
crash.log
crash.*.log
