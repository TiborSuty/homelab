apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: argocd
  namespace: argocd
spec:
  networkRouterRef:
    name: homelab-services
    namespace: netbird
  serviceRef:
    name: argocd-server
  groups:
    - name: dashboard-services
