apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vaultwarden

resources:
  - certificates.yaml
  - configmap.yaml
  - service.yaml
  - statefulset.yaml
  - network-resource.yaml
